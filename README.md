# 🚕 Такси по Южному

Веб-приложение для работы водителем такси и заказа такси в качестве пассажира.

## Возможности

- **Для водителей:**
  - Регистрация и вход в систему
  - Выход на линию / уход с линии
  - Автоматическое получение заказов по очереди
  - Принятие или отклонение заказов
  - Таймер на принятие заказа (1 минута)

- **Для пассажиров:**
  - Регистрация и вход в систему
  - Создание заказа такси
  - Отслеживание статуса заказа в реальном времени
  - Уведомления о назначении и принятии заказа водителем

## Технологии

- **Backend:** Flask, Flask-SQLAlchemy, Flask-SocketIO
- **Frontend:** HTML5, CSS3, JavaScript, Socket.IO
- **База данных:** SQLite (по умолчанию)
- **Real-time:** WebSocket через Socket.IO

## Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

### 3. Использование

1. Откройте браузер и перейдите на `http://localhost:5000`
2. Зарегистрируйтесь как водитель или пассажир
3. Для тестирования откройте два окна браузера:
   - В одном зарегистрируйтесь как водитель и выйдите на линию
   - В другом зарегистрируйтесь как пассажир и создайте заказ

## Логика работы очереди водителей

1. Водители выходят на линию и автоматически добавляются в очередь
2. Когда пассажир создает заказ, он автоматически назначается первому водителю в очереди
3. Водитель имеет 1 минуту на принятие заказа
4. Если водитель не принимает заказ в течение минуты или отклоняет его, заказ переходит к следующему водителю в очереди
5. После принятия заказа водитель удаляется из очереди до завершения заказа

## Структура проекта

```
taxi_po_yuzhnomu/
├── app.py              # Основной файл Flask приложения
├── config.py           # Конфигурация
├── models.py           # Модели базы данных
├── requirements.txt    # Зависимости Python
├── templates/         # HTML шаблоны
│   ├── index.html     # Главная страница (вход/регистрация)
│   ├── driver.html    # Панель водителя
│   └── passenger.html # Панель пассажира
└── static/            # Статические файлы
    ├── css/
    │   └── style.css  # Стили
    └── js/
        ├── app.js     # Общие функции
        ├── driver.js  # Логика водителя
        └── passenger.js # Логика пассажира
```

## API Endpoints

### Аутентификация
- `POST /api/register` - Регистрация пользователя
- `POST /api/login` - Вход в систему

### Водитель
- `POST /api/driver/online` - Выход на линию
- `POST /api/driver/offline` - Уход с линии
- `GET /api/driver/orders/current` - Получить текущий заказ
- `POST /api/driver/orders/<id>/accept` - Принять заказ
- `POST /api/driver/orders/<id>/reject` - Отклонить заказ

### Пассажир
- `POST /api/passenger/orders` - Создать заказ
- `GET /api/passenger/orders/<id>` - Получить информацию о заказе

## WebSocket события

### От сервера к клиенту
- `new_order` - Новый заказ назначен водителю
- `order_assigned` - Заказ назначен водителю (для пассажира)
- `order_accepted` - Заказ принят водителем
- `order_timeout` - Время на принятие заказа истекло
- `queue_updated` - Очередь водителей обновлена

### От клиента к серверу
- `connect` - Подключение к серверу
- `disconnect` - Отключение от сервера

## Карта на странице заказа

- По умолчанию используется **OpenStreetMap** (Leaflet). Чтобы включить **Яндекс.Карты**, задайте переменную окружения:
  ```bash
  export YANDEX_MAPS_API_KEY=ваш_ключ
  ```
  Ключ: [developer.tech.yandex.ru](https://developer.tech.yandex.ru/) → JavaScript API и HTTP Геокодер.
- На странице заказа: карта максимально увеличена, поля «Откуда»/«Куда» сделаны компактнее. Рядом с каждым полем — кнопка **↺** для сброса точки, чтобы заново выбрать место на карте.

## Лицензия

MIT
